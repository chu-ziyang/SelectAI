# GitHub Personal Access Token for `npm run release`
# 生成地址：https://github.com/settings/tokens （选 classic，勾选 repo scope）
# 把这个文件复制为 .env.local 并填入真实 token（.env.local 已在 .gitignore）
GITHUB_TOKEN=ghp_replace_me_with_real_token
